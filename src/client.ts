let clientFiles: string[] = [
  'a1.jar',
  'b1.jar',
  'c1.jar',
  'e1.jar', // Only on client
]
import cli from 'cli-ux'

const ip = ''
const port = 3456

export function main() {
  console.log('Client')
}
